// Shared constants used across multiple data modules and components

// Base path for all local image assets (depends on Vite BASE_URL)
export const IMG_BASE = `${import.meta.env.BASE_URL}images/`

// Appended to search terms to scope Google Maps/Search results to Walt Disney World
export const WDW_SUFFIX = ' Walt Disney World'
